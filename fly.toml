app = "reelrwa-frontend"
primary_region = "iad"

[build]
  [build.static]
    root = "dist"
    html_glob = "**/*.html"

[env]
  NODE_ENV = "production"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0
